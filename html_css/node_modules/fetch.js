// fetch cho phep gui request den sever
// syntax
// goi den API danh sach san pham
// producing code asynchornous operation
const products = fetch(
    'https://dummyjson.com/products',
{
    method:'GET';
}
)
// chi tiet san pham
const product = fetch(
    'https://dummyjson.com/products/1',
    {
        method: "GET"
    }
)

product.then(res =>res.json())
.then(res => console.log(res));
// xu li response api
// chuyen doi response tu json -> object
products.then(response=>{

})
// xu li response object va hien thi thong tin len website
.then(res => {
    console.log(res);
    // tao ra string html tuong ung
    let html='';
    for(const product of res.products){
        html +=`
            <tr>
            <td>${product.id}</td>
            <td>${product.title}</td>
            <td>
            <img src="${product.thumbnail}"
            </td>
            </tr>`

    }
    // update noi dung cua element id = productTableBody
    document.getElementById('productTableBody').innerHTML=html;
})
console.log(1);